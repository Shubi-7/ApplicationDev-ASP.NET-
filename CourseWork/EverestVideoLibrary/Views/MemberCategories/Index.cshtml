@model IEnumerable<CourseWork2.Models.MemberCategory>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";
}

<h2>Member Categories</h2>

<div class="m-4">
    <a class="btn btn-outline-primary" href="@Url.Action("Create", "MemberCategories")">Add New Member Catagory</a>
</div>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TotalLoan)
        </th>
        <th>
            Action
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TotalLoan)
        </td>
        <td>
            <a href="@Url.Action("Edit" , new { id = item.Id })" class="btn btn-dark"> Edit </a>
            <a href="@Url.Action("Details" , new { id = item.Id })" class="btn btn-dark"> Detail</a>
            <a href="@Url.Action("Delete" , new { id = item.Id })" class="btn btn-dark"> Delete</a>
        </td>
    </tr>
}

</table>
